function h = plot_pdf(observations,edges,opts)

switch nargin
    case 0
        error('Need at least 1 input!');
    case 1
        edges = linspace(nanmin(observations),nanmax(observations),10);
end

if isvector(observations) && isrow(observations)
    observations = observations';
end

[N,num_var] = size(observations);

[counts,bins] = hist(observations)
prob_mass = sum(counts,1);
counts = counts./prob_mass(ones(

% for i = 1:num_var
%     [counts] = hist(observations(:,i),edges);
%     counts = counts/nansum(counts);
%     hold on
%     bar(edges,counts);
%     
%     h = findobj(gca,'Type','patch');
%     
%     if nargin > 2
%         set(h,opts{i,:});
%     end
% end
% hold off

end

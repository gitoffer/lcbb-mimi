function [mean,all] = mean_increasing_time(rate,1)

if ndims(rate) > 2, error('Dimensions greater than 2 not supported.'); end

if nargin < 2, dim = 1; end

if dim == 1, rate = rate'; end

n_cols = size(rate,1);
padded = [